# 更新记录

## 0.3.3

- Chrome 仍在运行但 `DevToolsActivePort` 丢失时，从经过校验的本机浏览器状态恢复连接
- 恢复逻辑只接受仍在运行、资料目录一致且绑定 `127.0.0.1` 的健康端点
- 增加真实 MCP 端口记录丢失后的第三客户端恢复测试

## 0.3.2

- 每个 MCP 客户端使用独立临时输出目录，避免控制台日志和错误页快照进入长期缓存
- 客户端结束时完整删除临时输出，异常退出留下的目录由后续启动器安全清理
- 增加临时目录范围、清理行为和真实 MCP 退出清理测试

## 0.3.1

- 修正多个全新任务同时启动时，等待方把正常等待误判为浏览器已经退出的问题
- MCP 冒烟测试改为两个客户端同时从全新资料目录启动并完成读取

## 0.3.0

- 独立 Chrome 改为本机单例，多个 Codex 任务通过 CDP 连接同一浏览器
- CDP 使用随机端口并只绑定 `127.0.0.1`
- 增加跨进程启动锁、端点健康检查、陈旧锁恢复和浏览器程序路径配置
- 每个任务使用自己的标签页序列，禁止操作其他任务标签页
- MCP 冒烟测试增加两个客户端同时连接同一临时资料的真实验证

## 0.2.0

- MCP 子进程固定在仓库外的输出目录运行，禁止相对文件写入插件目录
- 页面快照固定通过标准输出返回
- 启动器移除 `browser_snapshot filename`，防止临时链接和原始页面结构落盘
- 增加 `about:blank` 页面丢失恢复规则，禁止把空白标签直接误判为未登录
- 增加快照文件名拦截和仓库外运行目录测试

## 0.1.0

- 建立 AIALRA Shopping Browser 插件
- 固定微软官方 `@playwright/mcp` 版本并使用独立持久 Chrome 资料
- 增加人工登录与验证交接规则
- 增加统一浏览器证据 Schema、敏感字段检查和 URL 清理工具
- 增加插件验证、单元测试和本地 MCP 冒烟测试
