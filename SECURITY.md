# 安全说明

## 数据保存

插件代码仓库不保存账号、密码、Cookie、令牌、验证码、浏览器资料、页面原文、截图、下载文件或运行日志

持久 Chrome 资料和临时输出保存在仓库外的本地目录

底层组件产生的控制台日志和错误页快照只进入当前客户端的私有临时目录

客户端结束时删除整个临时目录；进程异常退出时由后续启动器确认原进程结束后清理

删除 Chrome 资料目录会同时删除这个独立浏览器中的登录状态

## 浏览器权限

本插件启动独立 Chrome，不连接用户日常 Chrome，不安装要求 `<all_urls>`、`cookies` 或 `debugger` 权限的自定义扩展

独立 Chrome 的 CDP 端口随机生成并只绑定 `127.0.0.1`

CDP 没有单独登录界面，因此本机账户访问权限、资料目录权限和不向局域网或公网绑定端口属于必要安全边界

多个 Codex 任务只连接这一浏览器单例，不复制登录资料或导出存储状态

MCP 可以操作独立 Chrome 中当前打开的网页，所以使用者应把这个浏览器仅用于研究任务

不要在该浏览器中打开网银、邮箱、密码管理器、企业后台或其他与研究无关的敏感页面

## 允许的操作

- 打开公开或用户有权访问的页面
- 输入搜索词
- 读取搜索结果、详情、公开评价和公开评论
- 展开不会写入账户的内容
- 选择只影响页面展示且不会加购、领券或提交的规格
- 截取必要的证据画面

## 禁止的操作

- 下单、支付、加购、领券、出价、议价或联系卖家
- 点赞、收藏、关注、评论、私信、举报或发布内容
- 读取或导出 Cookie、密码、本地存储、浏览器历史和设备指纹
- 自动处理验证码、滑块、扫码和双重认证
- 使用代理轮换、指纹伪装、隐身补丁或其他规避平台保护的方法
- 在宿主或工具明确拒绝访问后切换手段绕过

## 人工交接

出现登录或验证页面时，Agent 必须停止自动操作并说明当前页面需要用户处理

用户完成后，Agent 只从当前页面继续

验证失败、限流或策略阻止不能自动重试

## 漏洞报告

不要在公开 Issue 中粘贴登录资料、网页原文、私人链接、截图或日志

报告应只包含最小复现步骤、插件版本、操作系统、错误类型和已经脱敏的错误信息
